# authorized-scope-recon
Conservative, scope-respecting asset-inventory tool for authorized bug-bounty programs. Passive subdomain enumeration + httpx liveness checks by default; optional robots-aware crawl and redacted, unverified JS secret-pattern scanning. GET-only — never exploits, brute-forces, or validates findings.
